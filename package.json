{
  "name": "parama-lk",
  "private": true,
  "type": "module",
  "scripts": {
    "upload:r2": "node scripts/upload-r2.mjs",
    "deploy": "npx wrangler pages deploy . --project-name=parama --branch=main",
    "preview": "npx wrangler pages dev ."
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
